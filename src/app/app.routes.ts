import { Route } from '@angular/router';
import { PortfoliosComponent } from '@ng-concepts/portfolios';

export const appRoutes: Route[] = [
    {
        path: '', component: PortfoliosComponent
    }
];
